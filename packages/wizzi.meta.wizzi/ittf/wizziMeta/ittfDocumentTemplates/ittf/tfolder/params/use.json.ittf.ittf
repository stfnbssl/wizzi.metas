$group
    ${'$'}params name
    {
        name use"${'$'}{_.capitalize(name)}"
        type "boolean"
    { 
        name ${'$'}{name}
        type "object"
        [ properties
            ${'$'}hook