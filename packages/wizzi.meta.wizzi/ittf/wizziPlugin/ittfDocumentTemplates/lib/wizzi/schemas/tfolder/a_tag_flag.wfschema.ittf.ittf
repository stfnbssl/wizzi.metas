$group
    ${'$'}params name, tag, defaultValue|false
    a ${'$'}{name}
        tag ${'$'}{tag}
        type boolean
        default ${'$'}{defaultValue}
        default-when-declared true
        ${'$'}hook