$group
    ${'$'}params name, tag|@@null
    a ${'$'}{name}
        ${'$'}if tag
            tag ${'$'}{tag}
        type boolean
        default false
        default-when-declared true
        ${'$'}hook