$group

    $
        var items = [
            "main",
        ];

    $foreach item in items
        $file __dot__wizzi/ittf/t/layouts/${item}.html.ittf.ittf
            ${'$'}{'$'}group
                ${'$'}include webStaticSite/tfolder/layouts/${item}.html