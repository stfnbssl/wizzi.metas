$group
        $
            var items = [
                "constants",
            ];

        $foreach item in items
            $file __dot__wizzi/components/${item}.ts.ittf.ittf
                module
                    ${'$'}include tsNextjsBasicApp/components/${item}.ts
