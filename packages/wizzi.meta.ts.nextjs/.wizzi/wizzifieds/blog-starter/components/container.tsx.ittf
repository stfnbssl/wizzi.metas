module
    :type Props
        :{ 
            :p children
                :optional 
                :ref React.ReactNode
    const Container
        => 
            { 
                @ children
                :ref Props
            return 
                div 
                    @ className "container mx-auto px-5"
                    + {children}
    export-default Container
