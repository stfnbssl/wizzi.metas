module
    kind react
    import 
        @ useRouter
        from 'next/router'
    import Nav from '../components/Nav'
    const SlugPage
        => 
            const 
                { 
                    @ asPath
                = useRouter()
            return 
                < React.Fragment
                    < Nav 
                    p 
                        + Hello, I'm the
                        + {asPath}
                        + page
    export-default SlugPage
