module
    kind react
    const withBundleAnalyzer
        _ require('@next/bundle-analyzer')
            { 
                @ enabled process.env.ANALYZE === 'true'
        #
            # * @type {import('next').NextConfig}
    const nextConfig
        { 
        #
            # * @type {import('next').NextConfig}
    set module.exports = withBundleAnalyzer(nextConfig)
