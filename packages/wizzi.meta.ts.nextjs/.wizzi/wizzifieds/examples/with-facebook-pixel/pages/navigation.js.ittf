module
    kind react
    import Link from 'next/link'
    export-default 
        function NavigationPage
            return 
                div 
                    p 
                        + Navigating between pages will trigger a pageview event in Facebook&lf;        Pixel, but will not reinitialize the pixel.
                    < Link 
                        @ href "/"
                        + Return to home
