module
    kind react
    export-default 
        function Index
            return 
                p 
                    + To test the CORS route, open the console in a new tab on a different&lf;      domain and make a POST / GET / OPTIONS request to
                    b 
                        + /api/cors
                    + . Using&lf;      a different method from those mentioned will be blocked by CORS
