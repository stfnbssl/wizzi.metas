module
    kind react
    const withPreact = require('next-plugin-preact')
        #
            # * @type {import('next').NextConfig}
    const nextConfig
        { 
        #
            # * @type {import('next').NextConfig}
    set module.exports = withPreact(nextConfig)
