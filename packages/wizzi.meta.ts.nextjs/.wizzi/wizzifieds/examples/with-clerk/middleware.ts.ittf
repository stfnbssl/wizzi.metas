module
    kind react
    import 
        @ authMiddleware
        from '@clerk/nextjs'
    export-default 
        _ authMiddleware
            { 
                [ publicRoutes
                    @ '/'
                    @ '/api/getAuthenticatedUserId'
    export 
        const config
            { 
                [ matcher
                    @ '/((?!.*\\..*|_next).*)'
                    @ '/'
