module
    kind react
    import 
        @ AppProps
        from 'next/app'
    import '../global.css'
    export-default 
        function MyApp
            { 
                @ Component
                @ pageProps
                :ref AppProps
            return 
                < Component 
                    @ {...pageProps}
