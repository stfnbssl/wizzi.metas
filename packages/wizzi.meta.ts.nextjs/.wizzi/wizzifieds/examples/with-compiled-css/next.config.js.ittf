module
    kind react
    set module.exports =
        { 
            @ webpack
                => 
                    param config
                    _ config.module.rules.push
                        { 
                            @ test /\.js$/
                            [ use
                                @ '@compiled/webpack-loader'
                    return config
        #
            # * @type {import('next').NextConfig}
