module
    kind react
    import 
        @ createClient
        from 'nhost-js-sdk'
    export 
        const nhost
            _ createClient
                { 
                    @ baseURL process.env.NEXT_PUBLIC_BACKEND_URL
