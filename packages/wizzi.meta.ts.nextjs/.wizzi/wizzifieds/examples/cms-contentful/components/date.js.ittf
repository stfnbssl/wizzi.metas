module
    kind react
    import 
        @ format
        from 'date-fns'
    export-default 
        function DateComponent
            { 
                @ dateString
            return 
                time 
                    @ dateTime {dateString}
                    + {format(Date(dateString), 'LLLL	d, yyyy')}
