module
    kind react
    export-default 
        function Container
            { 
                @ children
            return 
                div 
                    @ className "container mx-auto px-5"
                    + {children}
