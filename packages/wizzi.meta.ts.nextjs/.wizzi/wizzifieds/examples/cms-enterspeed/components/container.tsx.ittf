module
    kind react
    :type Props
        :{ 
            :p children
                :ref React.ReactNode
    export-default 
        function Container
            { 
                @ children
                :ref Props
            return 
                div 
                    @ className "container mx-auto px-5"
                    + {children}
