$group
    name "${cliCtx.pkgName}"
    pluginBasename "${verify.isUndefined(pluginBasename) ? cliCtx.pkgName : pluginBasename}"
    npnName "${verify.isUndefined(pluginBasename) ? cliCtx.pkgName : "@wizzi/plugin." + pluginBasename}"
    description "${cliCtx.pkgName}"
    version "0.0.1"
    { author
        name "Stefano Bassoli"
        email "stfn.bssl@gmail.com"
    { license
        name "MIT"
        copy "copy 2023"
    { github
        userid "stfnbssl"

