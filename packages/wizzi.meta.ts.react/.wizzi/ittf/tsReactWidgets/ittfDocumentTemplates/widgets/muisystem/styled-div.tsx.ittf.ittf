$group
    import 
        as React
        from 'react'
    import 
        @ styled
        from '@mui/system'
    +
    const Div
        _ styled('div')
            (
                { 
                    @ color 'darkslategray'
                    @ backgroundColor 'aliceblue'
                    @ padding 8
                    @ borderRadius 4
    export
        function StyledDiv
            param props
            return 
                < Div 
                    + {props.children}