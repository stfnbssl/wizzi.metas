$group

    const cors =  require('cors')

    # see options here https://expressjs.com/en/resources/middleware/cors.html
    const options
        {
    
    set module.exports = {}
    set module.exports.CorsMiddleware
        => 
            param app
            _ app.use
                _ cors
                    @ options