$group

    ${'$'}
        var schema = md.schema;

    {
        @ kind "flex"
        @ direction "row"
        [ items
            {
                @ kind "panel"
                @ id "${'$'}{schema.name}-list-container"
            {
                @ kind "panel"
                @ id "${'$'}{schema.name}-form-container"
