$group
    ${'$'}params &md
    ${'$'}
        var defaultProps = [];
    +
    misc${'('} setMyProps, &md )
    +
    import styled
        @ keyframes
        @ css
        from 'styled-components'
    ${'$'}hook imports
    +
    const md = module.exports = {}
    +
    set md.${'$'}{md.name}
        _ styled.${'$'}{md.element}
            => 
                param props
                    :any
                ${'$'}hook
