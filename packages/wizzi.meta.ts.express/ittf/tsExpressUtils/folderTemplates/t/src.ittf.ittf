$group

    $include src_utils
    
    $
        var items = [
        ];

    $foreach item in items
        $file __dot__wizzi/src/${item}.ittf.ittf
            module
                ${'$'}include tsExpressUtils/src/${item}