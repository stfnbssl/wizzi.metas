$group

    ${'$'}
        var ctrl = {
            name: 'DemoWebpack',
            path: '/demo/webpack',
            kind: 'site'
        }

    v08/ts/controller${'('} &ctrl )
        
        _ this.router.get(`/hello`, this.hello)
        
        ${'$'}append handlers

            v08/ts/handler${'('} hello )
                _ response.render
                    @ 'demoWebpack/hello.html.ittf'
                    {
                        @ title 'Wizzi webpack hello'