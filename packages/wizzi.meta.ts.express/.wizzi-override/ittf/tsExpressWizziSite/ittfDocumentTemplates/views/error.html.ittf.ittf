$group

    ${'$'}
        var opt = {
        }

    layouts/site${'('} &opt )

        . w3-container
            h1 Sorry, We got an error
            p ${'$'}{message}
            pre
                code
                    + ${'$'}{JSON.stringify(error, null, 2)}