$group
	$$ folder controllers ext ts
	$
		var items = [
			'apiv1wizzifs',
		];

	$foreach item in items
		$file __dot__wizzi/src/features/wizziFs/controllers/${item}.ts.ittf.ittf
			module
				${'$'}include tsExpressFeatureWizziFs/controllers/${item}.ts