$group

    $
        var items = [
            "PackiBuilder.ts",
        ]

    $foreach item in items
        $file __dot__wizzi/src/features/packi/api/${item}.ittf.ittf
            module
                ${'$'}include wizziDevPacki/src/features/packi/api/${item}

