$group

    $include packi_api

    $
        var items = [
            "index.ts",
            "types.ts",
        ]

    $foreach item in items
        $file __dot__wizzi/src/features/packi/${item}.ittf.ittf
            module
                ${'$'}include wizziDevPacki/src/features/packi/${item}

