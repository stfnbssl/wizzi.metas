$group

    $
        var items = [
            "package.json"
        ];

    $foreach item in items
        $file __dot__wizzi/root/${item}.ittf.ittf
            {
                ${'$'}include tsCLI/root/${item}

    $
        var items = [
            "env.example.text"
        ];

    $foreach item in items
        $file __dot__wizzi/root/${item}.ittf.ittf
            text
                ${'$'}include tsCLI/root/${item}